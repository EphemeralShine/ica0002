$TTL	604800
{% for vm in groups['dns_servers'] %}
{{ startup_name }}.	IN	SOA	{{ vm }}.{{startup_name }}. danka.{{startup_name }}. (
			      2		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;
{{ startup_name }}.	IN	NS	{{ vm }}
{% endfor %}

{% for vm in groups['all'] %}
{{ vm }}	IN	A	{{ hostvars[vm]['ansible_default_ipv4']['address'] }}
{% endfor %}

{% for vm in groups['db_servers'] %}
db IN CNAME {{ vm }}
{% endfor %}

{% for vm in groups['web_servers'] %}
nginx IN CNAME {{ vm }}
{% endfor %}